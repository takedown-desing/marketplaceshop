# OS
.DS_Store
Thumbs.db

# Секреты — никогда в репозиторий
.env
.env.*
*.key
*.pem
.secrets/

# Зависимости и сборка
node_modules/
dist/
web/node_modules/
web/dist/
.wrangler/
.cache/

# Медиа контекста (созвоны, записи) — держим локально
*.mp4
*.mov
*.wav

# Внутренние документы: анализ конкурентов, ценовая логика, блокеры.
# Репозиторий публичный — сюда они не едут. Лежат локально и в артефакте.
docs/
